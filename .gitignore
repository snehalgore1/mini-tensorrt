build/
build-*/
cmake-build-*/
.cache/
compile_commands.json

# python
__pycache__/
*.pyc
.venv/
venv/

# generated models and fixtures are regenerable, but keep goldens small and committed
models/*.bin
models/*.onnx
!models/goldens/
# ...except the tiny MLP fixture: committed so the C++ test suite needs no Python (D8)
!models/mlp.bin

# profiling output
*.trace.json
perf.data*
*.dSYM/

# editors
.vscode/
.idea/
.DS_Store
